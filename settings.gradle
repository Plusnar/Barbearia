pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Barbearia"

// Aqui indicamos que o módulo :app está dentro da pasta android/app
include ':app'
project(':app').projectDir = new File('android/app')
